name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  # Backend Testing
  test-backend:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd backend
        pip install -r requirements.txt

    - name: Run linting
      run: |
        cd backend
        pip install black ruff
        black --check .
        ruff check .
      continue-on-error: true

    - name: Run tests
      run: |
        cd backend
        pip install pytest pytest-asyncio
        # pytest tests/  # Uncomment when you add tests
      continue-on-error: true

  # Frontend Testing
  test-frontend:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: |
        cd frontend
        npm ci

    - name: Run linting
      run: |
        cd frontend
        npm run lint
      continue-on-error: true

    - name: Build frontend
      run: |
        cd frontend
        npm run build

  # Docker Build Test
  docker-build:
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]

    steps:
    - uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build Docker image
      run: |
        docker build -t supportflow:${{ github.sha }} .

    - name: Test Docker image
      run: |
        docker run -d -p 8000:8000 \
          -e OPENAI_API_KEY=test-key \
          -e DATABASE_URL=sqlite:///./test.db \
          supportflow:${{ github.sha }}
        sleep 10
        curl -f http://localhost:8000/health || exit 1

  # Deploy to Render (on push to main)
  deploy-render:
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend, docker-build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
    - name: Trigger Render deployment
      run: |
        echo "Render auto-deploys from main branch"
        echo "Deployment will start automatically"
        # Optional: Add Render deploy hook here
        # curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}

  # Optional: Deploy to AWS ECS
  # deploy-aws:
  #   runs-on: ubuntu-latest
  #   needs: [test-backend, test-frontend, docker-build]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #
  #   steps:
  #   - uses: actions/checkout@v3
  #
  #   - name: Configure AWS credentials
  #     uses: aws-actions/configure-aws-credentials@v2
  #     with:
  #       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #       aws-region: us-east-1
  #
  #   - name: Login to Amazon ECR
  #     id: login-ecr
  #     uses: aws-actions/amazon-ecr-login@v1
  #
  #   - name: Build and push Docker image
  #     env:
  #       ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
  #       IMAGE_TAG: ${{ github.sha }}
  #     run: |
  #       docker build -t $ECR_REGISTRY/supportflow:$IMAGE_TAG .
  #       docker push $ECR_REGISTRY/supportflow:$IMAGE_TAG
  #
  #   - name: Deploy to ECS
  #     run: |
  #       aws ecs update-service --cluster supportflow --service supportflow-service --force-new-deployment
